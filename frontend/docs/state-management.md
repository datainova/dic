# State Management — Frontend

Opções recomendadas
- React Query / TanStack Query — para cache de dados assíncronos e sincronização com backend
- Zustand ou React Context — para estado local/temporal do app

Patterns
- Use React Query para fetch/caching de dados de indicadores
- Use context/zustand para preferências do usuário e UI state
