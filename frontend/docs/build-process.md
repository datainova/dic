# Build Process — Frontend

Local
- `npm run dev` — Vite dev server
- `npm run build` — build para produção (pasta `dist`)

CI/CD
- Configurar pipeline que roda tests, lints e `npm run build`
- Deploy estático para Render ou CDN
