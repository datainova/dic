{
  "name": "dic-root",
  "private": true,
  "scripts": {
    "start:local": "./scripts/start-local.sh",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint && npm --prefix worker run lint",
    "test": "npm --prefix frontend run test -- && npm --prefix backend run test && npm --prefix worker run test",
    "build": "npm --prefix frontend run build && npm --prefix backend run build && echo 'Worker build skipped'"
  }
}
